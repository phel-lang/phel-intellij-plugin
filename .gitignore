.gradle
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/
.kotlin

### IntelliJ IDEA ###
.idea/
*.iws
*.iml
*.ipr
out/
!**/src/main/**/out/
!**/src/test/**/out/

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/
!**/src/main/**/bin/
!**/src/test/**/bin/

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

### VS Code ###
.vscode/

### Claude Code ###
.claude/settings.local.json
.claude/scheduled_tasks.lock

### OMC ###
.omc/

### Mac OS ###
.DS_Store

.intellijPlatform

# >>> agnostic-ai (managed) >>>
# Generated paths. Edit specs, not this block. Run `agnostic-ai sync` to refresh.
# Not committed: a fresh clone or `git worktree` lacks these until `agnostic-ai sync` runs (e.g. a post-checkout hook).
/.agents/skills/
/.agnostic-ai/.sync-state
/.agnostic-ai/packs/
/.claude/agent-memory-local/
/.claude/agents/
/.claude/commands/
/.claude/rules/
/.claude/settings.json
/.claude/settings.local.json
/.claude/skills/
/.codex/agents/
/.codex/config.toml
/AGENTS.md
/CLAUDE.md
/agnostic-ai.local.yaml
# <<< agnostic-ai (managed) <<<
# Plugin signing artifacts — the certificate and private key live in CI secrets, never in git.
*.pem
*.crt

# Local sandbox scratch project for ./gradlew runIde. Internal testing only; never shipped.
manual-test/
